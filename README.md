Afin d'être présent dans le PATH, les scripts présents dans ce
répertoire peuvent par exemple être ajoutés à `~/.local/bin` via un
lien symbolique (`ln -s REF SHORTCUT` pour créer un lien vers `REF`).
